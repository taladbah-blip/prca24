kind = "web"
previewPath = "/"
title = "PRCA-24 Arabic"
version = "1.0.0"
id = "artifacts/prca24-ar"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/prca24-ar run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prca24-ar", "run", "build" ]
publicDir = "artifacts/prca24-ar/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
